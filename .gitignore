node_modules/
.worktrees/
*.tsbuildinfo
.env*
!.env.example

# `maker make-offer` writes a signed offer here by default, so running the CLI inside the repo leaves one
# behind. They are per-run artifacts carrying a maker signature; none of them belongs in history.
offer-*.json
